name: crawler
description: Keeps every OpenSpecs document alive on a set of relays.
version: 1.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.7.0
  ndk: ^0.9.0
  path: ^1.9.0
  sembast: ^3.8.9+1
  sync_engine_shim_for_ndk: ^0.3.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
